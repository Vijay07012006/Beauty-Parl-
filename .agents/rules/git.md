---
trigger: always_on
---

# Git Push Strategy Rule

Always push commits and hotfixes directly to the `main` branch unless explicitly asked otherwise by the user. Do not use feature branches for pushes.
